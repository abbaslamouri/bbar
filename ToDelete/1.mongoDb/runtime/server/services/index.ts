import { MongoClient, ServerApiVersion } from 'mongodb'

// const config = useRuntimeConfig()

// // export const mongoDbClient = new MongoClient(config.mongoDbUrl)

// export const mongoDbClient = new MongoClient(process.env.NUXT_MONGO_DB_URL as string, {
//   serverApi: {
//     version: ServerApiVersion.v1,
//     strict: true,
//     deprecationErrors: true,
//   },
// })
